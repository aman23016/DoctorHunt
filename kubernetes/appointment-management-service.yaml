# apiVersion: v1
# kind: Service
# metadata:
#   name: appointment-management-service
# spec:
#   selector:
#     app: appointment-management  # This matches the label of the appointment-management pod
#   ports:
#     - protocol: TCP
#       port: 80  # Expose externally on port 80
#       targetPort: 5003  # Map to the internal port 5003 in the container
#   type: LoadBalancer  # Useful for cloud environments
apiVersion: v1
kind: Service
metadata:
  name: appointment-management-service
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: '80'       # Service port (not targetPort)
    prometheus.io/path: '/metrics' # Metrics endpoint
spec:
  selector:
    app: appointment-management
  ports:
    - port: 80          # Service port
      targetPort: 5003  # Pod port (where /metrics is served)
  type: LoadBalancer
